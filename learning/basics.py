#Startgerüst vorgegeben:
temps = [21.3, 22.1, 23.0, 22.8]    # Liste der Temperaturen

def max_temp(values):       # Funktion zur Bestimmung der maximalen Temperatur
    return max(values)      # Verwenden der eingebauten max-Funktion

print(max_temp(temps))      # Ausgabe der maximalen Temperatur